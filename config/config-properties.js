
class ConfigProperties{
    static HEADLESS = "true";
    // static HEADLESS = false; //enable if u want to see what browser is doing
    static MIN_THREADS = 2;
    static MAX_THREADS = 4;
}

module.exports = ConfigProperties
